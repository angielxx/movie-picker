// 월드컵의 종류들
// 1. 모든 영화

const playGame = (MovieIdList) => {
    if (MovieIdList.length === 1) return MovieIdList[0];
    
    // 부전승 처리 (홀수일 때)
    if (MovieIdList.length % 2) {
        
    }
}

const verse = (a, b) => {

}

export default